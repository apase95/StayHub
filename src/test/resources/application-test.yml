spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true
    baseline-on-migrate: false

  thymeleaf:
    cache: false

app:
  mail:
    enabled: false
  bootstrap:
    admin:
      enabled: false
